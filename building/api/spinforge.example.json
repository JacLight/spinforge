{
  "$schema": "https://build.spinforge.dev/_api/customer/manifest/schema",

  "appId": "app_b5354edd-e287-480f-b750-72b28a39a255",
  "repo": {
    "url": "https://github.com/me/my-app"
  },

  "$comment": [
    "Download this pre-filled from your dashboard (open the app, then",
    "'Download manifest') and commit it to your repository root.",
    "",
    "The app must already exist. A manifest points at an app you own; it",
    "can never create one or claim a domain. appId is stable across domain",
    "changes, so this file never needs editing after a rename.",
    "",
    "Optional: repo.ref, repo.token, rootDir, autoDeploy.",
    "",
    "Deploy:",
    "  curl -X POST https://build.spinforge.dev/_api/customer/manifest \\",
    "       -H 'Authorization: Bearer $SPINFORGE_TOKEN' \\",
    "       -H 'Content-Type: application/json' \\",
    "       --data @spinforge.json"
  ]
}

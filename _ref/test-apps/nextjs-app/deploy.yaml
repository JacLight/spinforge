name: nextjs-app-upload
version: 1.0.0
domain: nextjs-upload.localhost
customerId: customer1
framework: nextjs

build:
  command: npm install && npm run build
  env:
    NODE_ENV: production

resources:
  memory: 512MB
  cpu: 0.5

env:
  NODE_ENV: production
  PORT: 3000